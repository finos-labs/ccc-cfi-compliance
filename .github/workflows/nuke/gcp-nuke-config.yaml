blocklist:
  - "000000000000" # Prevent accidental deletion in wrong project

regions:
  - global
  - all

accounts:
  "nodal-time-474015-p5":
    filters:
      IAMWorkloadIdentityPool:
        - "finos-oidc-pool"
      IAMServiceAccount:
        - type: contains
          value: "gha-deployer"
      IAMWorkloadIdentityPoolProvider:
        - "github-provider"
